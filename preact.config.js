export default (config, env, helpers) => {
  // let proxy = helpers.getLoadersByName(config, 'proxy-loader');
  // const sass = proxy[1];
  // const { includePaths } = sass.loader.options.options;
  // sass.loader.options.sassOptions = { includePaths };
  // delete sass.loader.options.options.includePaths;
};
